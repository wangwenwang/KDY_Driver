//
//  QRImageView.swift
//  newDriver
//
//  Created by wenwang wang on 2018/12/10.
//  Copyright © 2018年 凯东源. All rights reserved.
//

import UIKit

class QRImageView: UIImageView {

    /*
    // Only override draw() if you perform custom drawing.
    // An empty implementation adversely affects performance during animation.
    override func draw(_ rect: CGRect) {
        // Drawing code
    }
    */
    
//    override func hitTest(_ point: CGPoint, with event: UIEvent?) -> UIView? {
//
//        let view:UIView = super.hitTest(point, with: event)!
//        if(view == nil) {
////            let newPoint:CGPoint =
//            print("fsd")
//        }
//
////        UIView *view = [super hitTest:point withEvent:event];
////        if (view == nil) {
////            // 转换坐标系
////            CGPoint newPoint = [_deleteButton convertPoint:point fromView:self];
////            // 判断触摸点是否在button上
////            if (CGRectContainsPoint(_deleteButton.bounds, newPoint)) {
////                view = _deleteButton;
////            }
////        }
////        return view;
//
//        return view
//    }
}

